syntax = "proto3";

package pb;

// 生成的 Go 代码会用到这个路径作为包名
option go_package = "./pb;pb";

// 定义一个 gRPC 服务
service HelloService {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// 请求消息
message HelloRequest {
  string name = 1;
}

// 响应消息
message HelloReply {
  string message = 1;
}
